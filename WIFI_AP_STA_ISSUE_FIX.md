# ESP32 车机桥接 Wi‑Fi 问题排查与修复说明

本文档总结本项目中“ESP32 连接上游热点 + 对外发 AP，但车机提示网络不可用”的问题定位与修复过程。

## 1. 现象

典型现象：

- ESP32 STA 能拿到上游 IP（例如 `10.x.x.x`）
- AP 客户端（车机）也能拿到 DHCP 地址（例如 `192.168.4.x`）
- 但部分 App（QQ 音乐、酷狗）提示“当前无网络”
- 部分 App（如视频类）偶尔可用

## 2. 根因分析

这类问题通常不是“完全断网”，而是 **多层链路下的兼容性问题**：

1. AP+STA+NAT 架构下，车机对网络可用性的探测更严格。
2. 仅设置 netif DNS 不一定等于 DHCP 会把 DNS 正确下发给客户端。
3. 某些车机/应用只认 DHCP Option 6（DNS Server）信息。
4. Wi‑Fi 省电策略在 AP+STA+BT 共存时会导致瞬时时延抖动，音乐类应用更敏感。

## 3. 已落地修复项

### 3.1 Wi‑Fi/NAT 与 DNS

- 启动 AP+STA，并在 STA 获取 IP 后：
  - 同步上游 DNS 到 AP netif
  - 启动 SNTP
  - 启用 NAPT（若配置开启）
- 当上游备 DNS 不可用时，使用公共 DNS 回退。
- 增加 DHCP Option 6 的 DNS 下发配置，提升车机兼容性。

### 3.2 连接稳定性

- 关闭 Wi‑Fi 省电模式（`WIFI_PS_NONE`）以提升首连稳定性与实时流媒体表现。

### 3.3 蓝牙内存压力

- 增加 `ENABLE_MEDIA_PROFILES` 宏（默认关闭 A2DP/AVRCP），缓解 Classic BT 内存压力，降低 RFCOMM `malloc failed` 风险。

### 3.4 通话记录时间

- 将 PBAP 通话记录时间从 UTC 处理改为本地时区处理。
- 增加 `LOCAL_TIMEZONE`（默认 `CST-8`）并在启动时设置 `TZ` + `tzset()`。

## 4. 关键配置检查清单

每次烧录后请优先检查：

1. `WIFI_STA_SSID`、`WIFI_STA_PASS` 已改为真实值
2. `CONFIG_LWIP_IPV4_NAPT` 已开启
3. 分区表容量足够（避免 app 超分区）
4. 手机上游热点为 2.4GHz，WPA2/WPA 混合优先

## 5. 运行日志判定

### 正常链路关键日志

- `Wi-Fi STA 已连上，IP=...`
- `AP DNS(main) 跟随 STA: ...`（或回退提示）
- `DHCP DNS Option 已设置: ...`
- `AP NAT 已开启...`

若这几条都出现，且手机连接 ESP32 AP 可正常上网，说明链路基本可用；剩余多为车机/应用判网策略差异。

## 6. 若仍“网络不可用”

建议顺序：

1. 车机端清除该热点记录后重连（获取新 DHCP/DNS）
2. 对比测试：手机连同一 AP 是否可稳定上网
3. 观察车机是否频繁断开重连（reason=3 常见为客户端主动离开）
4. 必要时固定公共 DNS（主/备）并观察日志

## 7. 结论

本项目已从“链路不通”推进到“车机应用兼容优化”阶段。
当前方案在大多数场景可用，个别应用是否显示“网络可用”仍可能受车机 ROM 的联网探测策略影响。
